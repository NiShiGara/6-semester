{% extends 'base.html.twig' %}

{% block title %}EmployeePosition{% endblock %}

{% block body %}
    <h1>EmployeePosition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ employee_position.id }}</td>
            </tr>
            <tr>
                <th>ФИО работника</th>
                <td>{{ employee_position.fio }}</td>
            </tr>
            <tr>
                <th>Должность</th>
                <td>{{ employee_position.employeePost}}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_employee_position_index') }}">back to list</a>

    <a href="{{ path('app_employee_position_edit', {'id': employee_position.id}) }}">edit</a>

    {{ include('employee_position/_delete_form.html.twig') }}
{% endblock %}
